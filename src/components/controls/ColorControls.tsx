import { Tabs, Text } from "@radix-ui/themes";

export default function ColorControls() {
  return (
    <Tabs.Content value="color-controls">
      <Text>Color Controls</Text>
    </Tabs.Content>
  );
}
